name = "cml-lesson-generator"
main = "worker.js"
compatibility_date = "2024-11-01"

# Secrets (ANTHROPIC_API_KEY, PASSPHRASE) are NOT set here.
# Set them with `wrangler secret put NAME` — see README.md.

[vars]
# Change this to your real site origin once you know it, e.g.
# ALLOWED_ORIGIN = "https://yourname.github.io"
ALLOWED_ORIGIN = "*"
